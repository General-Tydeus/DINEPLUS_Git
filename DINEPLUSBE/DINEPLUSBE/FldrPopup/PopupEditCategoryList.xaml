<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
                xmlns:animations="clr-namespace:Rg.Plugins.Popup.Animations;assembly=Rg.Plugins.Popup"
                  x:Class="DINEPLUSBE.FldrPopup.PopupEditCategoryList"
             CloseWhenBackgroundIsClicked="False">
    <pages:PopupPage.Animation>
        <animations:ScaleAnimation
            DurationIn="400"
            DurationOut="300"
            EasingIn="SinOut"
            EasingOut="SinIn"
            HasBackgroundAnimation="True"
            PositionIn="Center"
            PositionOut="Center"
            ScaleIn="1.2"
            ScaleOut="0.8" />
    </pages:PopupPage.Animation>
    <StackLayout
        Margin="5"
        Padding="24"
        HeightRequest="500"
        WidthRequest="300"
        BackgroundColor="WhiteSmoke"
        HorizontalOptions="Center"
        VerticalOptions="StartAndExpand">
        <StackLayout>
            <Label x:Name="lblTitle" Text="Category" Grid.Row="0" Grid.Column="0" HorizontalOptions="Center" FontSize="Medium"/>

            <ListView x:Name="LVCategoryList" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <ContentView Padding="5">
                                <Frame OutlineColor="DodgerBlue"
                               Padding="5">
                                    <StackLayout Orientation="Horizontal">
                                        <Label x:Name="lblCatCode" Text="{Binding CatCode }" VerticalTextAlignment="Center" HorizontalOptions="Start" IsVisible="False"/>
                                        <Label x:Name="lblCatDesc" Text="{Binding CatDesc }" VerticalTextAlignment="Center" HorizontalOptions="Start" />
                                    </StackLayout>
                                </Frame>
                            </ContentView>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
        <Button x:Name="btnClose" BackgroundColor="Crimson" Clicked="BtnClose_Clicked"
            CornerRadius="10" FontSize="Default" IsEnabled="True" Text="Close"
            TextColor="White" Grid.Row="2" Grid.Column="1"/>
    </StackLayout>
</pages:PopupPage>